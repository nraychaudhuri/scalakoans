package org.functionalkoans.forscala

import support.KoanSuite

class AboutPromiseForBetterFuture extends KoanSuite {

  koan("""A future is an abstraction which represents a value 
    which may become available at some point""") {
  }

  koan("Future uses the Hollywood principle - don't call me, I will call you") {
  }

  koan("Compose multiple small asynchronous tasks") {

  }
  
  koan("Should not block when you have to use blocking") {
    
  }

  koan("Promise completes a future") {
    
  }

}
